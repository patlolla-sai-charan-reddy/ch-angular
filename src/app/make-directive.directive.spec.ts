import { MakeDirectiveDirective } from './make-directive.directive';

describe('MakeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
